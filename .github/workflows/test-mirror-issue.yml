name: Test mirror-issue (reusable)
on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Mirror issue number (must have "mirror" label and issue form body)'
        required: true
        type: number

permissions:
  issues: write

jobs:
  mirror:
    uses: hermeto-fullsend/workflows/.github/workflows/mirror-issue.yml@mirror-issue-workflow
    with:
      upstream_repo: hermetoproject/hermeto
      mirror_issue_number: ${{ inputs.issue_number }}
    secrets:
      variable_token: ${{ vars.VARIABLE_TOKEN }}
